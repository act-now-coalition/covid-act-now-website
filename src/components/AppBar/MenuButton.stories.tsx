import React from 'react';
import MenuButton from './MenuButton';

export default {
  title: 'Building Blocks/Buttons',
  component: MenuButton,
};

export const HamburgerMenu = () => <MenuButton />;
