import React from 'react';
import { Region } from 'common/regions';
// import { MobileOnly, DesktopOnly } from 'components/NewLocationPage/Shared/Shared.style';

const GetAlertsBlock: React.FC<{ region: Region }> = ({ region }) => {
  return <div>hello</div>;
};

export default GetAlertsBlock;
