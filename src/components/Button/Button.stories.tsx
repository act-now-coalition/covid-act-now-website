import React from 'react';
import { BaseButton } from './Button.style';

export default {
  title: 'Components/Buttons',
  component: BaseButton,
};

export const Base = () => (
  <BaseButton href="https://covidactnow.org">Covid Act Now</BaseButton>
);
