import { Region, State, County, MetroArea } from 'common/regions';

export function getShareQuote(overallScore: number, region: Region): string {
  const baseText =
    'Communities across the U.S. are not all equally vulnerable to the effects of the pandemic. According to @CovidActNow, ';
  return 'hi';
}
