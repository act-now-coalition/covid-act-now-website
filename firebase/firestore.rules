rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /alerts-subscriptions/{email} {
      allow write;
    }
    match /snapshots/{snapshot_id}/locations/{fips}/emails/{email} {
      allow write;
    }
  }
}
